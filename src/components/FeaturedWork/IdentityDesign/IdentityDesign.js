import React from 'react';
import Box from '@material-ui/core/Box';
import Core from './Core/FeaturedWork';

const IdentityDesign = () => {
  return (
    <Box>
      <Core />
    </Box>
  );
};

export default IdentityDesign;
